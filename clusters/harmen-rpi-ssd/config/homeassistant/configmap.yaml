apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homeassistant

configMapGenerator:
  - name: ha-config
    files:
      - definitions # include all files in definitions/ into the ConfigMap

generatorOptions:
  disableNameSuffixHash: true
